# PW.1.1 foundations & framework — Dev Log

> Detailed working log for this task. Append chronological entries below.
> On task completion, append a one-line milestone to the **top-level**
> `agents/dev_log.md` (the canonical narrative).

## 2026-08-02

- Task scaffolded. Split out of the former PW.1 (which combined all 7 sketch
  items into one folder) per the user: "make each sub task small in size,
  separate PW_1 into more tasks, use same notation as other active tasks
  (e.g. P1_2)." PW.1 is now PW.1.1–PW.1.4; the alphabetic `PW_1b` suffix is
  dropped in favor of numeric sub-indices matching the repo convention
  (`<prefix>_<num>_<snake_desc>`).
- This slice owns the metric/prediction foundations + the per-interval framework
  loop (former sketch items 1, 2, 6, 7) **and** the Θ_i convention finding that
  gates PW.3/PW.4 Option A vs B.

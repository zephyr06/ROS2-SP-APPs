# SP-Metric Optimization — Overall Task List

> The TODO index. Active tasks point into `active_tasks/<Pn_n_name>/` (each has
> `goal.md`, `dev_log.md`, `tasks.md`); completed work is summarized in
> `finished_tasks/summary.md`; the canonical chronological narrative is
> `dev_log.md`. Priorities: P0 blocks publication / correctness; P1 is active
> investigation; P2 is should-do hygiene; P3 is deferred (let go for future).
> *Smaller value = higher priority.*

**Overall goal:** reliable, accurate figures for paper publication, within a
limited time budget. Two buckets: (1) **code development** — correct
functionality + reasonable algorithm design; bugs unacceptable, fewer features
okay; (2) **figures & analysis** — core figures are must-have (prove algorithms
work + show advantages over baselines), not all analysis is necessary.

---

## Active tasks

### P0 — blocks publication / correctness & clean baseline

| Task | Folder | One-line |
|------|--------|----------|
| ~~**P0.1** Persist adopted TL to YAML~~ | — | **RESOLVED 2026-07-10 (subsumed by P0.5; inspectability write discarded).** Functional TL-init bug fixed by P0.5 by construction (`CommitIncumbent` single-writes `res_opt_`; `BuildChallengerFromIncumbent` rebuilds the adopted-TL DAG → both diff sides carry the adopted TL → the P1.1 false-positive class structurally impossible; runtime-confirmed `ndiff` 5→0). The demoted inspectability remainder (overwrite the taskset YAML with the adopted TL after each commit, for post-run debuggability) was never implemented and is **discarded** — the orchestrator clamps job ET to `res.id2time_limit` (`SimulationOrchestrator.cpp:461-463`), so no scheduler decision honors the stale Gaussian once optimization has run. No code was ever written for P0.1. See `finished_tasks/summary.md`. |
| **P0.2** Focused BF correctness audit | [`active_tasks/P0_2_bf_correctness_audit/`](active_tasks/P0_2_bf_correctness_audit/) | Verify on one small fixed taskset that `OptimizeSP_TL_BF` enumerates the global optimum and `INCR ≤ BF`. Closes the "is BF optimal?" reviewer question. |
| **P0.3** Run prod pipeline + generate core figures | [`active_tasks/P0_3_prod_figure_run/`](active_tasks/P0_3_prod_figure_run/) | THE publication deliverable. Core figures 1a/1c/1f/ab_a/ab_b/2/3 + NEW `fig_p25_et_vs_period`. Depends on P0.5 (trustworthy incumbent) rather than P0.1. |
| **P0.4** Project evaluation suite (north-star integration test) | [`active_tasks/P0_4_project_evaluation_suite/`](active_tasks/P0_4_project_evaluation_suite/) | Slow (~20 min) deterministic integration test on N=4/6/8 — measures avg SP + scheduler ET vs the `project_evaluation_northstar.md` red-flag lines. The single tuning target for any code/algorithm change. Filed only; not started. |
| ~~**P0.5** Redesign optimizer iteration process (incumbent state)~~ | [`finished_tasks/P0_5_optimizer_iteration_redesign/`](finished_tasks/P0_5_optimizer_iteration_redesign/) | **RESOLVED 2026-07-10** (committed `a8dba07f`→`7fa2e9d2`; 46 `testIncreOpt_w_TL` + 16/16 ctest green, DEBUG re-verified at closeout). Architectural redesign of the incumbent STATE — owned once in `res_opt_` (no parallel `prev_optimizer_` cache), `CommitIncumbent`/`BuildChallengerFromIncumbent` helpers, transient challenger gated by `IfInitialized()` (the `has_incumbent_` bool was removed in Phase-5 issue 5d as provably redundant — `CommitIncumbent` is the single writer of `opt_pa_`). `prev_optimizer_` member removed; Phases 1–5 complete. Phase-5 review issues: 5a unified `ResetIncumbentBaseline`, 5c dropped the stale-TL guard, 5e renamed `starting_time_limits`, 5f removed dead `any_eval_ran` fallback, 5g total-budget patience; 5b kept rebuild-from-champion; 5h (efficiency) moved to P3.1. **Subsumes the functional TL-init bug by construction** — confirmed at runtime: the P1.1 INCR_P10 probe went `ndiff` 5 → 0 at call=0 (only ever 0 or 1 across all 302 calls). See `finished_tasks/summary.md`. |

### P1 — active investigation

| Task | Folder | One-line |
|------|--------|----------|
| **P1.1** P25 residual investigation | [`active_tasks/P1_1_p25_residual_investigation/`](active_tasks/P1_1_p25_residual_investigation/) | 3× ET growth eliminated but literal flip not met. Investigate first (reconcile 2-vs-8 changed-task-count; equal-radii A/B; confirm Fix D inert) — do NOT re-frame or implement Fix C yet. Reference: [`investigation/`](investigation/). |
| **P1.3** `ReoptStartFromAdoptedTL` A/B regression | [`active_tasks/P1_3_adopted_tl_regression/`](active_tasks/P1_3_adopted_tl_regression/) | First A/B with `INCR_P<n>_ADOPTED` arms (commit `8cbbbc12`) collapsed far below plain twins. **Root cause FOUND: stale `release/` binary** (built 2026-07-08, predates the 2026-07-11 commit; `run_end_to_end.sh` defaults `BIN_DIR=release` with no rebuild). Under the pre-commit code `_ADOPTED` is an unrecognized mode → dispatch fall-through → empty `ResourceOptResult` → period-independent degenerate schedule. Fingerprint: all four `_ADOPTED` arms produce byte-identical SP traces across P1/P10/P30/P60. Code itself verified sound (49 + 16/16 ctest green on the DEBUG build). **Step 2 DONE 2026-07-11**: `release/` rebuilt (mtime 11:26, after the commit) & verified fresh (`adopted` strings 0→3, mangled symbol linked); functional probe confirms the stale fingerprint is GONE — `INCR_P1_ADOPTED` SP 0.322→0.729, no longer byte-identical across periods at the collapsed SP. **Step 3 DONE 2026-07-11**: user re-ran the A/B on the fresh binary; `_ADOPTED` arms now distinct per-period and within 0.3–0.8 SP pts of plain twins (P1 0.5793→0.5710, P10 0.5597→0.5527, P30 0.5521→0.5491, P60 0.5456→0.5456 byte-identical), gap shrinking with reopt period. Verdict: stale-binary artifact, code sound — but the result seeded P1.4 (the incumbent seed is slightly worse; the user then required it anyway on algorithmic grounds). |
| **P1.4** Reopt seed TL = carried incumbent (permanent, unconditional) | [`active_tasks/P1_4_reopt_seed_from_incumbent/`](active_tasks/P1_4_reopt_seed_from_incumbent/) | User constraint: the reopt seed TL must be **algorithm-derived** (the optimizer's own prior result), not read from YAML or the generator. The old "off" default (`InitializeTimeLimitsFromETConfig`) is YAML-derived (`et_dist_` from `RegularTasks.cpp:61-106`; generator `mu` independent of the option grid). Closest algorithmic option = `ReconstructTimeLimitVecFromResOpt` (the `res_opt_` incumbent). **Implemented choice (b)**: REMOVED the `ReoptStartFromAdoptedTL` knob entirely (not kept as an ablation opt-out) — `ReOptimizePeriodic` now does `IfInitialized() ? ReconstructTimeLimitsFromResOpt() : InitializeTimeLimitsFromETConfig()` unconditionally; the `IfInitialized()` gate still auto-falls-back at interval 0 (fresh optimizer). Also REMOVED the `INCR_P<n>_ADOPTED` A/B arms + their parsing (a stale `_ADOPTED` config now fails LOUDLY in `MaybeOverrideReoptPeriod` rather than silently dispatching to an empty result — the P1.3 trap); the p25 config is 10→6 arms. TDD red→green (48/48 `testIncreOpt_w_TL` + 16/16 ctest). Accepted tradeoff: ~0.3–0.8 SP pts worse at high reopt frequency (→0 at P60). |
| ~~**P1.5** Add INCR_Px_INIT baselines (default seed)~~ | — | **RETIRED 2026-07-11 (invalidated by P1.4 choice (b)).** P1.5's premise was to add `INCR_P<n>_INIT` arms that set `ReoptStartFromAdoptedTL=false` to A/B the incumbent seed (P1.4's default) against the YAML/generator seed — the same A/B P1.3 ran via `_ADOPTED`. Choice (b) removes the flag entirely (the incumbent seed is the only reopt seed), so P1.5 is impossible as specified. The incumbent-vs-YAML-seed A/B is no longer runnable as a config arm; P1.3's measured tradeoff (~0.3–0.8 SP pts, →0 at P60) stands as the final record. Folder `active_tasks/P1_5_new_baseline_default_seed/` deleted. |
| **P1.6** Pure-incremental baseline (RM-fast bootstrap, no reopt) | [`active_tasks/P1_6_incr_only_baseline/`](active_tasks/P1_6_incr_only_baseline/) | **PLANNING ONLY 2026-07-11 (no implementation; plans updated in a follow-up).** A new paper-grade A/B arm: pure incremental — `OptimizeIncre_w_TL` (warm-started) every interval, **never** `ReOptimizePeriodic`, with the **first interval bootstrapped from RM-fast** (RM priorities + smallest-TL = the orchestrator's `RM_FAST`) instead of a from-scratch reopt. The interval-0 RM-fast seed already exists (`ResetIncumbentBaseline` `else` branch, `OptimizeSP_TL_Incre.cpp:437`); the arm = run that seed step, **skip** the `PerformCoordinateDescentForTaskConfigOpt` descent `ReOptimizePeriodic` runs afterwards, then pure incremental. NOT just `ReoptimizationPeriod=∞` (that still descends at interval 0). Purpose: clean incr-vs-scratch A/B — vs `INCR_SCRATCH` isolates the value of carrying the incumbent via warm-start (both bootstrap RM-fast); vs `INCR_P1` isolates whether the from-scratch descent earns its cost (both carry the incumbent). **INCR_P1-vs-INCR_SCRATCH distinction (resolved 2026-07-11):** both run the from-scratch descent every interval — the difference is optimizer lifetime: `INCR_P1` uses the persistent `incr_optimizer_` (carries `res_opt_` → compare-and-keep vs the running best), `INCR_SCRATCH` builds a fresh `scratch_opt` each interval (amnesiac → compare-and-keep vs a synthetic RM baseline); `INCR_P1` weakly dominates, `INCR_SCRATCH` is the no-memory control. The new arm keeps `INCR_P1`'s incumbent-carrying but drops the descent. **D2+D5 settled** (dispatch = a dedicated `scheduler_mode_` branch mirroring `INCR_SCRATCH`'s shape; paper-grade A/B probe, NOT an E3 gate). D1 (mode name), D3 (interval-0 mechanics), D4 (config placement) open. Also de-risks P1.2 (no `ReOptimizePeriodic` ⇒ the memoryless from-scratch search never runs ⇒ incumbent evolves only via the 1-D `OptimizeIncre` walk). Inherits P1.4's incumbent seed unchanged. See `goal.md`. |
| ~~**P1.7** Simulator ignores `processorId` partitioning (single run-queue overload)~~ | [`finished_tasks/P1_7_cpu_partition_mismatch/`](finished_tasks/P1_7_cpu_partition_mismatch/) | **RESOLVED 2026-07-12** (staged on `clean_simulation`, NOT committed; user runs the A/B re-run). Both FTP + CFS `SimulateInterval` now build one `RunQueue` (in `std::unique_ptr`) per distinct `processorId` via `GetProcessorIds`, lockstep-stepped; `ReleaseJobs`/`ReleaseJobsCFS` gained `int processor_id = -1` + the partition skip. Mirrors the legacy `SimulatedFTP_SingleCore` per-core model. 2 new red→green tests (`SimulateIntervalPartitionsByProcessorId` + `_CFS`) assert both cross-core jobs start at `t=0`; `testScheduleSimulate` 34/34, ctest 16/16 green; single-core tasksets byte-identical. **PIVOTAL CORRECTION:** the SP metric is **analytic** via `ProbabilisticRTA_TaskSet` (`RTA.cpp:100`) which already partitions on `processorId` → SP was **NEVER distorted**, **no gate verdict moves**, E3 unrelated. Only `job_history_`-derived miss-rate/response-time exports were distorted. **Residual follow-up filed as P2.6** (make SP consume RunQueue RTs). |
| **P1.8** INCR_WCET outperforms INCR in the eval-suite run (theoretically implausible) | [`active_tasks/P1_8_incr_wcet_outperforms_incr/`](active_tasks/P1_8_incr_wcet_outperforms_incr/) | **F1 CLAMP IMPLEMENTED + TDD-VERIFIED 2026-07-12 (staged on `clean_simulation`, NOT committed; user runs the A/B re-run).** User directive: "INCR_WCET outperforms other INCR tasks, this is theoretically highly unlikely, need to investigate why and fix this issue." In the N=4 eval-suite run, `INCR_WCET` Mean_SP_Metric=**0.5992** beat every plain INCR arm (0.5456–0.5710) at a *lower* miss rate — implausible for a *degraded* WCET-point-mass ablation (Q3 baseline). **Root cause = H1d generator defect (systemic):** `taskset_generator.py` draws `deadline = period*uniform(0.5,1.0)` independent of ET (no `deadline>ET` check) + `et_max = mu+2σ` uncapped vs deadline → 9/10 generated tasksets have a WCET>deadline task; the aggregate flip was dominated by ONE catastrophically-unschedulable taskset (taskset_1: INCR analytic SP collapsed to 0.0133 with ZERO actual misses while INCR_WCET held at 0.383). WCET collapse is value-copy-confined to the optimizer's search (does NOT reach scored SP); scored ET dists identical across arms → dominant scored-SP differentiator was the PRIORITY ASSIGNMENT (H1c = collapse mechanism on top of the H1d substrate). **F1 fix (user's simpler post-generation clamp design, greenlit):** new `Gen_Taskset/lib/feasibility_clamp.py` → `clamp_avg_et_to_period(yaml_dir, et_over_period_cap=0.95)`, wired as step 4 in `orchestrator.run_full_generation_pipeline` AFTER `generate_additional_execution_traces` returns. Walks every `taskset_characteristics*.yaml` (NOT `taskset_param.yaml`); for each non-perf task with `execution_time_mu > 0.95*period`: `mu`→`0.95*period`, `execution_time_max`→`min(max, 0.95*period)` (NEVER raises), `execution_time_min`→`min(min, new_max)`, `deadline`→`period`. Perf tasks SKIPPED (min/max are TL-grid bounds). Rewrite byte-compatible via `SpaceSeparatedListDumper`. **KEY:** clamping `mu` ALONE is a NO-OP on scored SP — `FiniteDist` (`Probability.cpp:18-43`) truncates the Gaussian at `execution_time_max`, so the scored support bound is `max`, not `mu`; the max/min clamp is mandatory (module docstring carries this so it can't be "simplified" back). 7 new tests RED→GREEN (`test_feasibility_clamp.py`) + invariant assertion in `test_integration_pipeline`; 21/21 Gen_Taskset + 291/291 `tests/python` green. Empirical re-scan: clamp fires on EXACTLY 1 task (taskset_1 gid=2 `mu=21.4>0.95*20`, the user's flagged `mu>period`); 39/40 already feasible — surgical, as designed. **REMAINS:** user re-runs the A/B to confirm INCR ≥ INCR_WCET on taskset_1+aggregate + Q3 verdict. H1c mechanism writeup now ACADEMIC (substrate removed at generation time). See `goal.md`/`dev_log.md`. |
| **P1.11** Incremental RTA Patching (priority-prefix reuse + per-core cache) | [`active_tasks/P1_11_incremental_rta_patching/`](active_tasks/P1_11_incremental_rta_patching/) | **SCOPE NARROWED 2026-07-18 to cache design + build only (Phase 0 DONE, working tree NOT committed).** Rev-3 single-champion `RTACache` class (`Initialize`/`AdoptChampion`/`Evaluate`/`ComputeTaskSetDifference`/`IsSingleTaskChange`/`ClassifyReusePerTask`) + leaf header `PrioritySwitchAnalysis.h` (`RestEqualAfterRemoving`/`FindCoreOfTask`/`AnalyzePrioritySwitchPerCore`/`AnalyzePrioritySwitch`) — all bit-identical to the `ProbabilisticRTA_TaskSet` oracle (differential TDD), 16/16 ctest + 46/46 testRTA green. The integration (wiring into the optimizer + dispatch in hot loops) was split out to **P1.12**; this folder remains the design doc for the cache API surface. |
| **P1.12** Integrate RTA cache into the incremental optimizer | [`active_tasks/P1_12_integrate_rta_cache/`](active_tasks/P1_12_integrate_rta_cache/) | **FILED 2026-07-18 (split from P1.11 Phase 1+2; no code yet).** Wire the P1.11 `RTACache` into the live incremental optimizer eval path. Phase 1 (baseline-only): add `RTACache rta_cache_` member to `OptimizePA_Incre_with_TimeLimits`, reset in `ResetIncumbentBaseline`, thread a required `RTACache&` from `EvaluateTimeLimitConfig_SubIncremental` down to `OptimizeIncre_SingleTask` (Hazard A — by reference, no slicing), `AdoptChampion` inside `CommitIncumbent` (`OptimizeSP_TL_Incre.cpp:699`), replace `EvaluateSPWithPriorityVec` with `rta_cache_.Evaluate(...)` + a named SP-assembly helper that multiplies `perf_coefficient` (Hazard B). Phase 2: dispatch the cache in Loop B (TL walk `OptimizeSingleTaskTimeLimit`) + Loop A (1D priority walk) + scalability measurement at N=6/10/16. Correctness gate: bit-identical to the `EvaluateSPWithPriorityVec` oracle (differential TDD). **UPDATE 2026-07-18: increment 2a DONE (working tree, NOT committed; 16/16 ctest green) — write-side only (`bool rta_cache_active_` gate + `AdoptChampion` in `CommitIncumbent`, gated; behavior-preserving, oracle still live).** The base-class `RTACache&` threading (Hazard A) was split out to **P1.13** as a standalone task. |
| **P1.13** RTA cache for priority optimization (standalone, base-class) | [`active_tasks/P1_13_rta_cache_priority_opt/`](active_tasks/P1_13_rta_cache_priority_opt/) | **FILED 2026-07-18 (split from P1.12's Hazard-A deferral; DESIGN ONLY, no code yet).** Wire the frozen P1.11 `RTACache` into the BASE priority optimizer `OptimizePA_Incre` (`sources/Optimization/OptimizeSP_Incre.{h,cpp}`) so `OptimizeIncre` reuses RTA across its 1D priority-assignment variations instead of a full `ProbabilisticRTA_TaskSet` per candidate. Each `FindPriorityVec1D_Variations` candidate moves ONE task's priority position vs the carried PA → `|diff|<=1` vs a champion whose PA == carried PA → the exact case `RTACache::Evaluate` serves. **`OptimizeFromScratch` is EXCLUDED** (user-stated: constructive beam search, invariant doesn't hold, no win). NOT touching `OptimizeSP_TL_Incre.h` (P1.12's domain). `api_design.md` drafts 6 open questions with recommendations: Q1 (OptimizeFromScratch cache-free), Q2=where cache lives (rec B: threaded `RTACache*` nullptr=oracle, opt-in behavior-preserving), Q3=champion lifecycle (adopt at `OptimizeIncre` baseline + per `OptimizeIncre_SingleTask` adoption), Q4=two new `ObtainSP_*` helpers (Hazard B — `perf_coefficient`; existing `ObtainSP_DAG_From_Dists` untouched, live caller), Q5=tl/dag plumbing (is the call-site DAG TL-baked? `UpdateExtDistBasedOnTimeLimit` == `ApplyTimeLimitsToTasksExecutionTime`?), Q6=no flag (pointer is the gate). Correctness gate: bit-identical to `EvaluateSPWithPriorityVec` oracle (differential TDD). Awaits user API sign-off before implementation. |
| **P1.10** Serialized single-task incremental optimization | [`active_tasks/P1_10_serialized_incremental_optimization/`](active_tasks/P1_10_serialized_incremental_optimization/) | **ALL DESIGN DECISIONS CLOSED 2026-07-17; PHASE 1 COMPLETE + COMMITTED (D1 sub-point 1 = `exclude_opt_pa` in `33b2270c`; D1-main `OptimizeIncre_SingleTask` extraction + `baseline_sp` arg + differential test in `3d2f9b28`; 16/16 ctest green); D2-amendment functions LANDED+TESTED in working tree (NOT committed); Phase 2 source awaits explicit user go.** Architectural redesign of the per-interval search loop — **this is the "more-important issue" P1.9 (`RTA_Cache`) is ON HOLD behind** (answers P1.9's Q5). Introduce a *sub-incremental* optimizer restricted to **one task's ET changing** (the `|diff|==1` specialization of `OptimizeIncre`'s loop body at `OptimizeSP_Incre.cpp:269-287`), then **serialize** the interval search: build the champion from `res_opt_`, merge the env-changed-ET (Type-E) + TL-flexible (Type-L) task sets, sort, and walk one task at a time. Type-E → sub-incremental (the perfect single-change fit); Type-L → the existing trial-and-error TL walk (`OptimizeSingleTaskTimeLimit` `:194`) but each TL step calls the sub-incremental optimizer (assuming all other tasks' ET unchanged vs champion → also a single-task change). The win: today every TL step re-runs the **full multi-task** `OptimizeIncre` via `EvaluateTimeLimitConfig_ScratchOrIncre` (`:142`); the serialized design walks each env-changed task **once** and keeps every per-candidate diff at `|diff|==1` → fewer SP-evals. **P1.9 unblock condition:** after P1.10 lands + proves the single-change invariant, P1.9's cache collapses to a trivial single-task patch every call → resume P1.9 then (do NOT touch P1.9 until P1.10's invariant is proven). **Decisions (all closed 2026-07-17):** D1 → new `OptimizeIncre_SingleTask(dag_tasks_update, task_id, et_increased)` (sub-point 1 `exclude_opt_pa=true` generator change LANDED in `33b2270c`; D1-main extraction + `baseline_sp` arg + differential test LANDED+COMMITTED in `3d2f9b28` — **Phase 1 COMPLETE**); D2 → `FindTaskWithDifferentEt` UNCHANGED (caller prepares both diff sides pre-TL) **THEN AMENDED** to a structural filter — two new free functions `FindTasksWithFlexibleTimeLimits` + `FindEnvTaskWithDifferentEt` (=`FindTaskWithDifferentEt` MINUS TL-flexible) LANDED+TESTED (NOT committed), because `FiniteDist::operator!=` is 10%-relative `approx_equal` (`Probability.cpp:415-417`) and a TL-flexible task's dist carries the raw YAML mu/min/max (no adopted-TL override at read, `RegularTasks.cpp:75-79`) → the "caller equalizes" premise is tolerance-dependent/fragile; `FindTaskWithDifferentEt` kept unchanged only for the live TL-walk `:282` call site; D3 → sort merged E+L queue by task weight descending; D4 → running-adopted single champion `res_opt_`; D5 → incremental-only first cut (the warm-started primitive fits incremental, NOT reopt's memoryless `OptimizeFromScratch`; + Type-E doesn't apply to reopt); #5 dedup → RaiseError if a task is both env-changed AND TL-flexible (disjoint by generator design; not a winner-pick); #6 baseline → dedicated re-score (NOT `EvaluateTimeLimitConfig_ScratchOrIncre`, which would optimize before the queue's sorted order is honored). 16/16 ctest green (DEBUG). See `goal.md`/`tasks.md`/`api_design.md`. |
| **P1.14** BF scheduler execution time violates its 10s `TIME_LIMIT` cap | [`active_tasks/P1_14_bf_time_limit_violation/`](active_tasks/P1_14_bf_time_limit_violation/) | **Phase 2 BF fix + Phase 2b INCR mirror IMPLEMENTED + TDD-green + COMMITTED at HEAD `bfbec7e5` (branch `clean_simulation`) 2026-07-19; Phase 3 A/B re-run BLOCKED on P1.15.** (Earlier "working tree, NOT committed / STAGED" framing in memory `p114-` was STALE — corrected 2026-07-19: the `BFDLSharedBudget` guard is committed, sites verified: BF `OptimizeSP_TL_BF.cpp:89`, INCR mirror `OptimizeSP_TL_Incre.cpp:653`, `UpdateSP` poll `OptimizeSP_Incre.cpp:71`.) In the P25 period A/B run (`tasks6_dur600_interval10_seed1000`), BF `Mean_Scheduler_Execution_Time_s = 24.38` vs ~0.2 s for every `INCR_Reopt_X` arm. Per-taskset BF totals: taskset_0 = ~182 s/interval (18× cap), tasksets 2/3/6/7/8 = ~10.1 s/interval, rest 0.5–5.5 s. `parameters.yaml:3` `TIME_LIMIT: 10`. **Root cause (CONFIRMED, both defects):** (1) inner `OptimizePA_BruteForce` constructs a fresh `OptimizePA_BF` per TL-leaf → per-leaf timer resets every leaf → aggregate runaway (taskset_0's 182 s); (2) `ifTimeout` checked only at recursion boundaries, never inside the leaf's SP-eval → one-leaf overshoot (~10.1 s cluster). **D1 RESOLVED =(a):** `TIME_LIMIT` bounds the WHOLE `EnumeratePA_with_TimeLimits` call per interval (one shared 10 s budget). **Fix (COMMITTED):** `BFDLSharedBudget` scope guard + `BFSharedBudgetCancelled()` in `OptimizeSP_Base.{h,cpp}`; `EnumeratePA_with_TimeLimits` installs ONE shared budget (seed = outer `start_time_`); both BF check sites switched from `ifTimeout(start_time_)` to `BFSharedBudgetCancelled()`; cooperative cancel inside `EvaluateSPWithPriorityVec` (returns `INT_MIN` on cancel → cancelled perm loses to incumbent, in-budget results byte-identical), `ObtainSP_DAG`, `ObtainSP_TaskSet`, `ProbabilisticRTA_TaskSet_SingleCore`; ms granularity. INCR mirror (`OptimizeSP_TL_Incre.cpp:653` + `UpdateSP` poll `OptimizeSP_Incre.cpp:71`) uses a FRESH `TimerType` per call (orchestrator reuses `incr_optimizer_` across intervals). Inert outside a `BFDLSharedBudget` scope. **TDD:** `tests/testBFRTimeout.cpp` + `tests/testINCRTimeout.cpp` (2 cases each, the single-eval-exceeds-cap case reproduces the taskset_0 signature synthetically); 17/17 ctest green in DEBUG `build/`. **Refactor follow-up flagged:** `ifTimeout` is now dead code — prunable. **REMAINING (BLOCKED on P1.15):** the P25 A/B used to verify is the same run where Reopt_X>1 arms SIGABRT — must land P1.15 (harness loud-failure + crash fix) first, then rebuild release binary + re-run A/B, confirm BF mean ≤ ~10 s + per-taskset totals under cap + SP unchanged for in-budget tasksets. See `goal.md`/`dev_log.md`/`tasks.md`. |
| **P1.15** Silent sim failure inflates the aggregated A/B (BF appears to lose) | [`active_tasks/P1_15_silent_sim_failure_inflates_agg/`](active_tasks/P1_15_silent_sim_failure_inflates_agg/) | **FILED + INVESTIGATION COMPLETE 2026-07-19; Phase 1 (harness loud-failure fix) IN PROGRESS per the user's chosen ordering (harness first, then crash investigation).** In the P25 period A/B run (`tasks4_dur600_interval10_seed1000`), `comparison_summary.csv` shows BF (0.6129) losing to INCR_Reopt_5/10/30/60 (~0.622) — but the Reopt_5+ rows report a suspicious `0.000000 ± 0.000000` miss rate (the fingerprint of empty input, not a real result). **User's hypothesis CONFIRMED: not an optimizer-correctness bug in the SP sense — a silent-failure + unequal-N aggregation bug in the Python harness.** Per-taskset × per-arm output map: BF + INCR_Reopt_1 produced `interval_sp_metrics.txt` on all 10 tasksets; Reopt_5/10/30/60 produced output on only 5 (missing on tasksets 0, 3, 6, 7, 8) — exactly the "hard" tasksets where BF beats Reopt_1. On taskset_0 BF and Reopt_1 are byte-identical (60/60 SP values, 0.672911, 4300/39900 misses). Two compounding harness defects: (1) `compare_optimizers.py:519-527` submits arms via `executor.submit` + `concurrent.futures.wait` with NO `.result()` → a `CalledProcessError` from `run_single_simulation`'s `subprocess.run(check=True)` is discarded; the analyze loop `:534-572` wraps `analyze_single_instance` in `try/except Exception` that only prints; (2) `run_sim_experiments.py:80` treats a missing `interval_sp_metrics.txt` as empty/zero (not raise), and `utils.py:206-218` does `np.mean` over **whatever survived** → means over unequal per-scheduler taskset sets (BF=600 values incl. hard tasksets; Reopt_5+=300 values excl. them) → manufactures BF's apparent loss. **Confirmed the binary aborts:** reproduced `taskset_3 INCR_Reopt_5/10/30/60` all exit 134 (SIGABRT, core dumped) on the CURRENT release binary (rebuilt 2026-07-19 14:49 from HEAD `bfbec7e5` — `git status` clean for `sources/`+`tests/`; earlier "NOT committed" framing in memory `p112-`/`p114-` was STALE — corrected). **Crash stack + throw site captured:** `RTA_Cache.cpp:357-364` (`ComputeTaskSetDifference` throws `std::runtime_error` when `TryComputeSingleChange` returns false → candidate differs from champion by >1 task, "violates the P1.10 single-change invariant"); stack `Evaluate → ClassifyReusePerTask → ComputeTaskSetDifference` thrown inside `EvaluateTimeLimitConfig_SubIncremental` (`OptimizeSP_TL_Incre.cpp:279`, the P1.12 read-side swap) within the serialized walk where `rta_cache_active_=true` (`:439`). So the abort is NOT a segfault — an uncaught C++ exception by design. **Decisions RESOLVED 2026-07-19 (user):** (1) **(A) harness first, then (B) crash investigation;** (2) the user's two explicit harness requirements — **stop the experiment when one optimizer crashes** (do not continue remaining arms/tasksets) + **show the crash loudly** (never a silent `0.000000` row; capture C++ stdout/stderr so the throw text survives for Phase 2; print a loud report; write `crash_report.txt` + per-taskset ✅/❌ map; exit non-zero); (3) aggregate = per-taskset-mean-then-average AND fail-loudly on unequal N; (4) `--resume` re-attempts partial/empty dirs. **Phase 1 (harness, layer A):** A0 TDD-RED (`tests/python/test_compare_optimizers_crash.py`: inject a crashing arm → run raises + no `0.000000` row + crash_report.txt written; `analyze_single_instance` raises on missing output; `write_summary_and_plots` emits NaN not 0.0 for empty arm) → A1 propagate `.result()` + stop-on-crash → A2 capture per-arm stdout/stderr to `run.log` → A3 `analyze_single_instance` raises on missing/empty output → A4 per-taskset-mean-then-average + NaN for missing arms + non-zero exit on unequal N → A5 `--resume` requires non-empty file → A6 loud end-of-run crash report → A7 GREEN. **Phase 2 (optimizer, layer B):** gdb `taskset_3 INCR_Reopt_5`, confirm throw site + which of 4 `TryComputeSingleChange` false-returns fires, identify WHY the champion drifts to >1 diff only at Reopt_X>1 (hypothesis: P1.14's cooperative-cancel returns INT_MIN → corrupts champion state?); fix = make throw impossible (prove |diff|<=1 invariant) or make `Evaluate` degrade gracefully on >1 (full recompute = cache-miss-not-crash, the safer immediate unblock) + TDD repro at smallest fixture. **Phase 3:** rebuild release binary, re-run P25 A/B on fixed binary+harness, verify every scheduler has N=10×60 + no `0.000000` rows + ✅/❌ table all ✅; unblocks P1.14 Phase 3. See `goal.md`/`tasks.md`/`dev_log.md`. |
| ~~**P1.16** RTA Cache Desynchronization on Rejected Walks~~ | [`active_tasks/P1_16_rta_cache_desync_fix/`](active_tasks/P1_16_rta_cache_desync_fix/) | **RESOLVED 2026-07-19.** Back up and revert the RTA cache champion state when a sub-incremental queue walk step is rejected by `UpdateRecords`. This prevents multi-change invariant violations and SIGABRT crashes on subsequent walk steps. Compiled and verified via C++ test suite (17/17 passed) and manual orchestrator run. |
| **P1.17** Remove redundant copy/recompute ops in RTA cache + main optimization code | [`active_tasks/P1_17_rta_cache_and_opt_redundant_ops/`](active_tasks/P1_17_rta_cache_and_opt_redundant_ops/) | **FILED 2026-07-19 (no code yet).** Refactor + perf (NOT correctness): audit and remove copy-paste / unnecessary operations accumulated across the RTA cache (`RTA_Cache.{h,cpp}`) + the TL/INCR optimizer eval path (`OptimizeSP_TL_Incre.{h,cpp}`, `OptimizeSP_Incre.{h,cpp}`). Initial inventory from the committed `Evaluate` body: (1) `Evaluate` re-bakes the CHAMPION DAG every call (`RTA_Cache.cpp:448-451`) though the champion is invariant across calls — cache it on `AdoptChampion`/`Initialize`; (2) `PerCoreOrderFromPa` rebuilt twice per `Evaluate` (by `ComputeTaskSetDifference` + `ClassifyReusePerTask` on the same candidate `(dag,pa)`); (3) `ExtractTaskSetPerProcessor` re-partitions what `PerCoreOrderFromPa` already partitioned; (4) `RTACache cache_backup = rta_cache_;` (`OptimizeSP_TL_Incre.cpp:~187`) copies the WHOLE cache per `EvaluateTimeLimitConfig_SubIncremental` entry even on the adopt path — narrow scope or move to reject branch; (5) `candidate_rta_.assign(size, FiniteDist({0,1.0}))` zero-inits every slot then overwrites each. **Gate = bit-identical SP to current committed code** (differential TDD, same contract as P1.12). Should land BEFORE P1.12 Phase 2 item 5's N=6/10/16 scalability measurement (task 3b here unblocks it) so the speedup number isn't dragged down by defensive copies layered in during P1.12's read-side swap + P1.12 item-1b reindex + P1.16's backup/revert. Sibling: **P1.18** (its task 1d helper is the seam P1.18 plugs into). See `goal.md`/`tasks.md`/`dev_log.md`. |
| **P1.18** Upgrade `ClassifyReusePerTask` to consider more optimization types | [`active_tasks/P1_18_classify_reuse_per_task_more_types/`](active_tasks/P1_18_classify_reuse_per_task_more_types/) | **FILED 2026-07-19 (no code yet).** `ClassifyReusePerTask` v1 emits a coarse per-task verdict: `FullReuse` (cross-core) or `NoReuse` (whole changed core). The reserved `RTAReusePerTask::ReuseHpTasksEt` slot (`RTA_Cache.h:41`, doc "future same-core-suffix refinement, NOT emitted by v1") + the cache's own dispatch-site note (`RTA_Cache.h:93-99`, `RTA_Cache.cpp:417-422`: "a future same-core-suffix refinement only needs ClassifyReusePerTask to emit ReuseHpTasksEt + a branch here") name the exact extension: **reuse the champion's HP-ET prefix up to `min(old_pos,new_pos)`, recompute only the suffix.** Cleanest first win = D3 (ET-only change, no priority move): tasks ABOVE the changed task reuse verbatim, only the changed task + the suffix below recompute — v1 marks all of them `NoReuse`. Open design questions D1–D5 in `goal.md` (is `hp_prefix_per_core_` the right prefix? does priority-move-within-core D2 yield a real saving? new verdict types beyond `ReuseHpTasksEt`?). **Gate = bit-identical SP to the oracle** (differential TDD, same contract as P1.11/P1.12). Reads the `old_pos`/`new_pos` locators v1 currently IGNORES. Recommended order: **P1.17 first** (its task 1d helper carries those positions), then P1.18. See `goal.md`/`tasks.md`/`dev_log.md`. |
| **P1.19** End-to-end pipeline `--rerun_mode` (clear-all / clear-results / resume) | [`finished_tasks/P1_19_e2e_rerun_mode/`](finished_tasks/P1_19_e2e_rerun_mode/) | **CLOSED 2026-07-24 — committed `8279ae0d` (Phase 1) + `f0858eef` (Phase 2).** Python-only harness knob (NOT correctness — changes WHEN prior artifacts are cleared, not HOW SP is computed). User request: a mode arg to decide whether to (1) remove ALL generated tasksets + re-run, (2) remove only simulation results + re-run results, or (3) resume (skip analysis already done). `apply_rerun_mode()` in the orchestrator runs BEFORE any stage (preempts the per-stage reuse/resume guards), respects `--dry_run` + `--verbose`; `--rerun_mode` CLI arg + `RERUN_MODE` env var forwarded from `run_end_to_end.sh` only when non-default; `reuse` default is a true no-op so bare `./run_end_to_end.sh` is unchanged. **Phase 1 (`clear_all`, commit `8279ae0d`):** `shutil.rmtree(<run_root>/sim/)` — wipes generated tasksets + per-scheduler results + sweep variants; does NOT touch sibling `figures/`. **Phase 2 (`clear_results`, commit `f0858eef`):** globs `tasks*_dur*_interval*_seed*/taskset_*/` and removes only the per-scheduler RESULT subdirs (`<taskset>/<scheduler>/`), keeping the flat taskset artifacts (`generator_config.json`, `*.yaml`, `path_Et_task_*.txt`) + the taskset dir — so `compare_optimizers`'s `--skip_generation_if_exists` reuses the tasksets while `--resume` re-runs every arm (A/B two binaries on identical tasksets without paying taskset regeneration). **TDD pin:** `tests/python/test_run_end_to_end_rerun_mode.py` 14/14 green (8 `clear_all` + 6 `clear_results` — the `clear_results` set pins "wipes per-scheduler dirs" AND "keeps flat taskset artifacts", the contract that distinguishes it from `clear_all`; enriched `setUp` with flat taskset artifacts so "keeps" is genuinely asserted); existing `test_run_end_to_end.py` still 17/17 green. **Phase 3 (`resume`) DEFERRED, reframed:** resume BEHAVIOR already exists via the separate, config-gated `--resume` flag (NOT a `--rerun_mode` choice) — `analysis.enable_resume_from_existing_results` (true in 4/6 configs) drives `build_simulate_command` (`run_end_to_end_experiments.py:163`) + the sweep (`interval_sweep.py:214`) to forward `--resume`; `compare_optimizers` skips arms whose `interval_sp_metrics.txt` exists AND is non-empty (`_has_complete_metrics`); the sweep ALSO has its own reuse logic (`_main_dir_is_fresh`, skips re-running compare_optimizers when a fresh `comparison_summary.csv` exists). What Phase 3 would ADD = unconditional resume (ignore the config flag) + stage-level skip for aggregate (which currently always regenerates figures). Convenience, not correctness; deferred unless a consumer needs it. Consumers (P1.14 Phase 3 / P1.15 Phase 3 / P1.17 3b) use `clear_all` to force fresh A/B runs. See `goal.md`/`tasks.md`/`dev_log.md`. |
| **P1.20** Convert processor→task-set maps from hash table to vector | [`finished_tasks/P1_20_processor_partition_map_to_vector/`](finished_tasks/P1_20_processor_partition_map_to_vector/) | **DONE 2026-07-23** (3 commits: C1 `484df475`, C2 `b20e5828`, C3 `337e9f8f`). Perf refactor (NOT correctness): every per-processor partition in the analytic RTA/SP path converted from `std::unordered_map<int, ...>` keyed by `processorId` → flat `std::vector<T>` indexed directly by `processorId` (O(1), no hash). **Investigation (key):** `RegularTasks.h:99` default `processorId = -1` proven NOT special-cased anywhere — purely a default hash key (`SimulationOrchestrator.cpp:465,668` `processor_id >= 0` is a caller arg from `GetProcessorIds`, never a branch on the field; `ExtractTaskSetPerProcessor`/`ProbabilisticRTA_TaskSet` treat `-1` as a plain key; `OptimizeSP_Base.cpp:95,99`+`OptimizeSP_Incre.cpp:76` compare equality only). **Decisions:** D1 normalize at source (`-1`→`0`); D2 fail-fast `ValidateProcessorIds()` in `DAG_Model` ctor (reject `<0` AND non-dense); D3 delete dead `task_id2task_index_within_processor_` (written, NEVER read); D4 `task_id2position_`/`task_id2index` OUT OF SCOPE (task_id-keyed, not processorId); D5 gate=bit-identical SP (TDD relaxed). **C1:** kill `-1`+validator+dead-code+2 stray non-dense fixtures (`test_robotics_v8/v27` core1→0). **C2:** vectorize `processor2taskset_` (`map`→`vector<TaskSet>`, internal-only — ZERO external readers, `GetProcessorIds` reads `dag_tasks.tasks` directly). **C3:** vectorize RTA layer (6 files: RTA+RTA_Cache+PrioritySwitchAnalysis+testRTA; `ChampionState::champ_per_core`/`hp_prefix_per_core`→`vector<vector<int>>`/`vector<vector<FiniteDist>>`; `PerCoreOrder*`→`vector<vector<int>>`; `FindCoreOfTask`/`AnalyzePrioritySwitch` take `vector<vector<int>>`). **2 correctness proofs:** (1) per-core RTA results INDEPENDENT (written scattered by task id, no cross-core accumulation) → core iteration order irrelevant → map→vector safe; (2) gapped-core test fixtures (absent core vs empty core) collapse to the same "zero tasks" under vector rep, matching old map `empty_vec` fallback → same verdicts. **C4 = verification-only (no code):** 4a EMPTY (testOptimizeIncrePA had only a stale comment, no fixture); 4b superseded — the 8/8 `*Differential*`/`*BitIdentical*` in-binary oracle probes run during C1–C3 are strictly stronger than a stdout stash→HEAD diff (same machine/libc, no timing noise); 4c perf spot-check NOT run (non-gate, provably neutral-to-faster). **Gate:** 17/17 ctest DEBUG green throughout + 8/8 oracle probes at every commit → SP provably neutral (pure storage change). **Stale-`.o` rule applied:** `--clean-first` after every header layout/member change. Sibling: **P1.17** (same hot path) + **P1.18** (D2 — task→core representation). See `goal.md`/`tasks.md`/`dev_log.md`. |
| **P1.21** Abstract RTA cache transactions using RAII | [`finished_tasks/P1_21_rta_cache_transaction_abstraction/`](finished_tasks/P1_21_rta_cache_transaction_abstraction/) | **DONE 2026-07-21; REVERTED 2026-07-23 by P1.25.** The `RTACache::Transaction` RAII (lazy CoW) this task added was removed by P1.25 — the transaction's perf premise was a timer artifact (P1.23) and 80% of captures were wasted (P1.22). P1.25 restored the `1217d227` eager (b) backup in its place. This entry preserved for the design/lesson trail. **(Original DONE note:)** Perf refactor (NOT correctness): scoped `RTACache::Transaction` RAII helper (lazy copy-on-write) replaces the P1.16 eager full-cache copy in `EvaluateTimeLimitConfig_SubIncremental`. **Phase 1+2a+2b+2c+3a+3c** = first cut: `struct ChampionState` (Memento over the 5 champion members; `candidate_rta_` DELIBERATELY excluded — scratch buffer, fully overwritten before read, cuts one of 3 heavy `vector<FiniteDist>` copies); `class RTACache::Transaction` (non-copyable, non-movable, no nesting; dtor restores iff `!committed_`); `SnapshotPreMutationStateIfOpen()` (first-capture-only) at the TOP of `AdoptChampion` AND `Initialize`; integration in `OptimizeSP_TL_Incre.cpp` (`RTACache::Transaction tx(rta_cache_);` at entry + `if (updated) tx.Commit();` after `UpdateRecords`); 3 cache-level pins in `testRTA.cpp` (compare `cache.Rta()` directly, NOT `Evaluate`, since Evaluate would hide a wrong champion). Accept-path ordering re-verified: `CommitIncumbent`'s `AdoptChampion` fires INSIDE `UpdateRecords` while the tx is still open → first-capture-only no-op → `tx.Commit()` retains it. **Phase 4** (decoupled redesign per Gemini's `design.md` + critique): moved tx state INTO the cache (`bool in_transaction_` + `std::unique_ptr<ChampionState> snapshot_`; dropped the redundant `has_snapshot_` bool — null-check on the pointer suffices) with explicit public API `BeginTransaction()`/`CommitTransaction()`/`RollbackTransaction()`/`InTransaction()`; `Transaction` is now a THIN RAII wrapper (no friend, no back-pointer) whose dtor is the SINGLE cleanup point (dispatches Commit vs Rollback — exception-safe); renamed `NotifyChampionAdoption()`→`SnapshotPreMutationStateIfOpen()` (honest verb; the old name lied). **Phase 5** (encapsulation per user "use ChampionState to encapsulate members"): `ChampionState` is now BOTH the live storage type AND the Memento — `RTACache::champion_` is one `ChampionState` member replacing the 5 loose ones, so `CaptureChampionState` collapses to `return champion_;` and `RestoreChampionState` to `champion_ = std::move(state);` (no hand-written field list to drift; a future 6th champion member cannot be silently forgotten by the snapshot). **Phase 6** (user "remove AdoptEvaluatedCandidate"): dropped the Phase-4c wrapper; `CommitIncumbent` inlines `Evaluate`+`AdoptChampion` (gated `rta_cache_active_`; "zero-cost reuse, not duplicate RTA" comment retained). **Gate: 17/17 ctest DEBUG green (17.6s clean) + testRTA 59/59 → bit-identical SP.** **Stale-build ABI postmortem (NOT a code defect):** a phantom `double free` in `BakeChampionForms` appeared against a half-rebuilt `build_test/` (stale `.o` from pre-Phase-5 header layout) → clean rebuild (`--clean-first` / `check.SP_OPT` target) eliminates it. Lesson: after any `RTA_Cache.h` LAYOUT change, build `--clean-first`; do NOT trust a partial incremental build's ctest. **3b (perf measurement) deferred** — per-outcome copy table already proves lazy ≤ eager every row, strictly better on reject-without-adopt (the dominant TL-descent case). Sibling: **P1.17** (same hot path; P1.17 item 4 was the `cache_backup` smell P1.21 closes), **P1.18**, **P1.20**. See `goal.md`/`tasks.md`/`dev_log.md`. |
| **P1.22** Investigate RTA cache transaction performance slowdown | [`finished_tasks/P1_22_investigate_rta_cache_transaction_slowdown/`](finished_tasks/P1_22_investigate_rta_cache_transaction_slowdown/) | **CLOSED 2026-07-23 — SUPERSEDED by P1.25.** Phase 1a frequency data: snapshot captures ~40% of opened tx (NOT 90–95%); 60% zero-copy reject (lazy-COW premise HOLDS); AdoptChampion 89% in-tx; heap alloc ~110/run NOT the cost (hypothesis 2 ruled out). **80.3% of captures thrown away by rollback** (530/660); only 19.7% kept. The "+5.02%@N=10 slowdown" premise was a timer artifact (P1.23: corrected timer → cache 36% faster, not slower). P1.25 removed the transaction layer entirely → the "does it pay for itself" debate is moot. TEMPORARY instrumentation + probe config reverted/deleted by P1.25 0b/0c/2b. See `goal.md`/`tasks.md`/`dev_log.md`. |
| **P1.25** Remove the RTA cache transaction layer (keep the cache) | [`finished_tasks/P1_25_remove_rta_cache_transaction/`](finished_tasks/P1_25_remove_rta_cache_transaction/) | **CLOSED 2026-07-23 — COMMITTED `0448db9c`.** Deleted the P1.21 `RTACache::Transaction` RAII (lazy CoW) + all machinery (`Transaction` class, `Begin/Commit/RollbackTransaction`, `SnapshotPreMutationStateIfOpen`, `in_transaction_`+`snapshot_`, P1.22 `TransactionCounters` instrumentation) from `RTA_Cache.h/.cpp`; removed the 3 `Transaction_*` unit tests from `testRTA.cpp` (pointer comment left). **D1=(b)** eager save/restore on the reject branch — restored the `1217d227` pre-transaction shape in `EvaluateTimeLimitConfig_SubIncremental`: `RTACache cache_backup = rta_cache_;` at entry + `if (!updated) { rta_cache_ = cache_backup; }` on reject. ACCEPT path stays tx-independent (`CommitIncumbent` re-adopts, `:791-793`). **TDD arc did NOT go red** — 1a/1b (`SubIncrementalReject/Accept_...` in `testIncreOpt_w_TL.cpp` via `RtaCacheExposingOptimizer` subclass) are no-regression GUARDS, not a red-then-green pin: the reject-after-adopt drift the backup guards is mechanically real but not exercised by the fixtures (the v19→v21 walk's single reject had no in-walk adopt). Backup load-bearing via git history (`1217d227`) + mechanism, NOT a live red. User chose option (I) (land on git-history proof); (a) re-seed-at-entry deferred as upgrade path. **Gate GREEN:** 17/17 ctest (`check.SP_OPT --clean-first`) + bit-identical SP to HEAD (stash 5 files → clean-build HEAD `testOptimizeIncrePA`+`testIncreOpt_w_TL` → capture → restore → rebuild (b) → capture → stdout diff with test-timing normalized = 0 lines; covers `Differential_BitIdenticalOnSingleEtChange` + `Differential_BitIdenticalToOracle_OnSingleEtChange`). See `goal.md`/`tasks.md`/`dev_log.md`. |


### P2 — should do (figure safety + doc hygiene)

| Task | Folder | One-line |
|------|--------|----------|
| **P2.1** Confirm Fig 2 sweep runs in prod | [`active_tasks/P2_1_fig2_sweep_confirmation/`](active_tasks/P2_1_fig2_sweep_confirmation/) | Stale-flags bug resolved in code (verified 2026-07-08: `compare_optimizers.py` accepts both `--on_taskset_config_change` and `--run_root`) but never confirmed on a full prod run. Can run during P0.3. |
| **P2.2** Doc & memory hygiene | [`active_tasks/P2_2_doc_memory_hygiene/`](active_tasks/P2_2_doc_memory_hygiene/) | Mark memory `interval-sweep-stale-flags-bug` RESOLVED; apply `investigation/` §5 corrections; `issues.md` #9 SUPERSEDED, #3/#6 let-go. Parallel anytime. |
| **P2.3** `FiniteDist::approx_equal` dead-code cleanup | [`active_tasks/P2_3_finite_dist_dead_code/`](active_tasks/P2_3_finite_dist_dead_code/) | `FiniteDist::approx_equal` (`Probability.cpp:345`) has zero production callers; `operator!=` hardcodes tolerance=1e-1 inline and doesn't delegate. Delete (default) or wire up. Cleanup left behind by the P1.1 "Fix D inert" finding. Parallel anytime. |
| **P2.4** Optimizer methods & mode-string refactor | [`active_tasks/P2_4_optimizer_methods_refactor/`](active_tasks/P2_4_optimizer_methods_refactor/) | **Slices A+B + 1-arg overload test migration COMPLETE 2026-07-11 (16/16 ctest DEBUG + 291 python green; `git add` staged, no commit).** Naming/clarity refactor (NOT a computation bug). Trigger: `INCR_P1` reads as "incremental" but is the MAX-reopt arm (`ReoptimizationPeriod=1` → reopt every interval, never incremental; the `P<n>` knob runs the wrong way for a reader). Issue catalogue I1–I5: I1 the `INCR_P1` inversion; I2 plain `INCR` vs `INCR_P1` silently coupled to the YAML `ReoptimizationPeriod` knob; I3 stale `prev_optimizer_` comments (P0.5 removed the member); I4 stale `ReoptStartFromAdoptedTL` history comments (P1.4 removed the flag); I5 method-name/role misalignment in `OptimizePA_Incre_with_TimeLimits`. **Shipped this pass:** Slice A (stale-comment rewrite) + Slice B (mode-string rename `INCR_P<n>`→`INCR_Reopt_X`, X∈{1,5,10,30,60}; X=5 NEW; stale `INCR_P<n>` FAILS LOUDLY — the P1.4 `_ADOPTED` pattern, not a silent alias; bare `INCR` stays canonical = `INCR_Reopt_10` via `parameters.yaml`) + the ~11 test/example 1-arg `ReOptimizePeriodic(K)` call sites migrated to the 2-arg `ReOptimizePeriodic(dag_tasks, K)` form. **DEFERRED:** Slice C (I5 method renames — conflicts with P1.6/P1.2 in-flight edits to `OptimizeSP_TL_Incre`). **Step 5 promoted to P2.5** (the `INCR_SCRATCH` removal + E2/north-star cleanup was result-changing and got its own review cycle; the 1-arg overload was deleted there together with the INCR_SCRATCH branch). Renames are behavior-preserving; the user runs the full new-name A/B. |
| **P2.5** Remove the `INCR_SCRATCH` scheduler arm | [`active_tasks/P2_5_incr_scratch_removal/`](active_tasks/P2_5_incr_scratch_removal/) | **COMPLETE 2026-07-11 (16/16 ctest DEBUG + 287 python green; `git add` staged, no commit).** Result-changing cleanup promoted from P2.4 Step 5. Deleted the `INCR_SCRATCH` dispatch branch (`SimulationOrchestrator.cpp`) + the 1-arg `ReOptimizePeriodic(int K)` overload (`OptimizeSP_TL_Incre.{h,cpp}` — its last caller was the SCRATCH branch; P2.4's `:428` test-site migration to 2-arg finished first). Collapsed the north-star from 6 gates to 5: **E2 deleted outright** (its only subject pair was INCR-vs-SCRATCH) and Q1/Q2/Q3/E1 narrowed to INCR-only; E3 (period-monotonicity) untouched. A stale `INCR_SCRATCH` config FAILS LOUDLY (dispatch falls through to the RM baseline — the P1.3 trap pattern, NOT a silent alias to `INCR_Reopt_1`). SCRATCH's amnesiac-reopt mechanism (the `IfInitialized()` interval-0 Gaussian-mean fallback) survives — it is exercised by ANY fresh-optimizer reopt, covered by `ReOptimizePeriodic_Interval0FallsBackToGaussianMean`; only the mode string + branch + the dead E2 gate went away. **P1.6 premise now obsolete** (it compared INCR vs SCRATCH); P1.6's re-framing deferred to the user. The user re-runs the A/B without INCR_SCRATCH. |
| **P2.6** Sim-RT-based SP metric (report both analytical + true SP) | [`active_tasks/P2_6_sim_rt_based_sp_metric/`](active_tasks/P2_6_sim_rt_based_sp_metric/) | **FILED 2026-07-12 (planning only; no implementation).** The SP metric the eval-suite gates read is **analytic** (`ObtainSP_TaskSet_And_TimeLimits` → `ProbabilisticRTA_TaskSet`, already partitioned per `processorId`); the simulation's `RunQueue` produces actual RT samples that SP currently ignores. Make the simulation report BOTH: the analytical SP (current, keep as the gate-facing metric) AND a true SP derived from the simulated per-job response-time samples in `job_history_` (a new `Mean_SP_Metric_Sim` / `mean_sp_norm_sim` column). Purpose: cross-check the analytic RTA against the actual schedule; quantify the analytic-vs-sim gap; unblock a future decision on whether SP should come from the schedule. **Prerequisite DONE** — P1.7 (per-`processorId` RunQueues producing honest RT samples, so the sim RTs are now trustworthy). Deferred for later (P2). |
| **P2.7** Code comment cleanup & simplification | [`active_tasks/P2_7_code_comment_cleanup/`](active_tasks/P2_7_code_comment_cleanup/) | **FILED 2026-07-23 (guiding rules defined).** Review and simplify code comments across `RTA_Cache.{h,cpp}`, `OptimizeSP_TL_Incre.{h,cpp}`, `OptimizeSP_Incre.{h,cpp}`, etc. Rules: (1) strip all task tags (`P1.x`), (2) explain WHY not WHAT (code is self-documenting), (3) 1-2 lines max (no multi-paragraph narrative essays), (4) delete stale/defensive changelog notes, (5) keep header contracts clean and impl notes minimal. See `goal.md`/`tasks.md`/`dev_log.md`. |

---

## Deferred / let go for future (P3)

Intentionally dropped from the publication path. *Features or perf, not
correctness* — acceptable under "fewer features okay."

| Task | Source | Why let go |
|------|--------|------------|
| **P1.2** Reopt incumbent degradation (structural corruption) | [`active_tasks/P1_2_reopt_incumbent_degradation/`](active_tasks/P1_2_reopt_incumbent_degradation/) | Spawned by the Gemini/Kimi debate. Out of scope in current stage of development. The possibility of committing an opportunistic but structurally worse/fragile priority assignment is kept as a known theoretical hazard. |
| Trial-and-error for **incremental priority** assignment | (was NEW TASK #1 in old tasks.md) | Algorithm feature; TL trial-and-error already shipped. Not needed to prove correctness or advantages. |
| `PriorityPartialPath` → pointers | [`P1_1_efficiency_optimizations/`](finished_tasks/P1_1_efficiency_optimizations/) §A | **CLOSED 2026-07-24** (let-go: "current speed is good enough"). Pure perf; no profiling evidence that ET is a paper blocker, so the task's own gate ("pick up only if P1.1 finds per-activation ET is a paper blocker") is satisfied by NOT picking it up. Stays deferred. |
| Incremental HP-task convolution O(N²)→O(N) | [`P1_1_efficiency_optimizations/`](finished_tasks/P1_1_efficiency_optimizations/) §2A | **CLOSED 2026-07-24** (let-go with the efficiency bucket). Already implemented in `ProbabilisticRTA_TaskSet_SingleCore` via the 3-arg `GetRTA_OneTask` overload (verified active + unit-tested); the priority-prefix reuse half later shipped as `P1.11` (`finished_tasks/P1_11_incremental_rta_patching/`). Perf, not correctness. |
| Reuse one challenger across `EvaluateTimeLimitConfig_ScratchOrIncre` calls | [`P1_1_efficiency_optimizations/`](finished_tasks/P1_1_efficiency_optimizations/) §3 | **CLOSED 2026-07-24** (let-go with the efficiency bucket). Moved from P0.5 Phase-5 issue 5h on 2026-07-10. Perf only. The rebuild-from-champion design (P0.5 5b) was chosen over the persistent challenger — drift risk to the diff baseline. Stays deferred. |
| Issue #3 — ignored `SP_THRESHOLD_RANGE` | `issues.md` #3 | Config flexibility; default `SP_THRESHOLDS_SET` works for paper. |
| Issue #6 — per-task-type config separation | `issues.md` #6 | Config flexibility; not needed for the paper's taskset family. |
| Debug figures 1B/1D/1E/3b | `plan_publication_figures.md` | Already classified Debug-only; not core. Keep code, don't polish for paper. |
| **P3.11** Partial task-subset optimization (X% per interval) | [`active_tasks/P3_11_partial_task_subset_optimization/`](active_tasks/P3_11_partial_task_subset_optimization/) | **FILED 2026-07-17 (design only).** Deferred to priority 3 (deferred/future). Walks only X% of the serialized E+L queue per interval, selection weight-biased + fair. |

---

## Completed

See [`finished_tasks/summary.md`](finished_tasks/summary.md) for the full
index (Pre-2026-07-01, 2026-07-01 P5–P9, P24/P25, trial-and-error TL).
Highlights: P24/P25 commits 1–7 + INCR-ET fix bundle committed (partial A/B
pass); trial-and-error TL rewrite done + TDD-green (pending commit in P0.1).

## Suggested execution order

~~P0.5~~ RESOLVED 2026-07-10 (committed `a8dba07f`→`7fa2e9d2`; subsumes the
functional TL-init bug by construction — see `finished_tasks/summary.md`) →
P0.2 (BF audit) → P0.3 (prod figures; depends on P0.5's trustworthy incumbent) →
P2.1 (fig2 confirm, during P0.3) → ~~P1.3~~ CLOSED 2026-07-11 (stale-binary
artifact; `_ADOPTED` A/B re-run on the fresh binary showed the arms within
0.3–0.8 SP pts of plain twins, gap shrinking with reopt period) → **P1.4**
(choice (b): incumbent seed made the unconditional, only reopt seed — the
`ReoptStartFromAdoptedTL` knob + the `_ADOPTED` arms REMOVED; user rebuilds
`release/` + re-runs the 6-arm A/B to confirm on the loaded tasksets) → P1.1
(P25 ET investigation, open research question) → ~~P1.5~~ RETIRED 2026-07-11
(invalidated by P1.4 choice (b); needed the removed flag) → P1.2 (reopt
incumbent-degradation investigation; reads the same P25 A/B data as P1.1 but
asks a different question — structural corruption, not ET growth; parallel
anytime until it needs a code change) → P2.2 (doc hygiene, parallel anytime)
→ P2.3 (`approx_equal` dead-code cleanup, parallel anytime).
~~P1.7~~ RESOLVED 2026-07-12 (staged on `clean_simulation`, not committed —
per-`processorId` RunQueues for FTP+CFS `SimulateInterval`; 2 new red→green
tests, `testScheduleSimulate` 34/34, ctest 16/16 green; **PIVOTAL: SP was never
distorted — analytic partitioned RTA — so no gate verdict moves**, only
miss-rate/response-time exports were; residual "SP from RunQueue RTs" filed as
P2.6; user runs the A/B re-run).
**P2.6** (FILED 2026-07-12, planning only — make the simulation report BOTH the
analytical SP and the true SP derived from the simulated `job_history_` RT
samples; prerequisite P1.7 DONE; deferred for later).
**P1.8** (F1 CLAMP IMPLEMENTED + TDD-VERIFIED 2026-07-12, staged on
`clean_simulation` not committed — user runs the A/B re-run. INCR_WCET
outperformed INCR in the N=4 eval-suite run, theoretically implausible; the
degraded WCET ablation beating the real optimizer. Root cause = H1d generator
defect (systemic, 9/10 tasksets have a WCET>deadline task); the flip was
dominated by ONE catastrophically-unschedulable taskset (taskset_1: INCR's
analytic SP collapsed to 0.0133 with **zero** actual misses, while INCR_WCET
held at 0.383). H1d=substrate, H1c (priority-assignment)=collapse mechanism.
F1 = the user's simpler post-generation clamp: new
`Gen_Taskset/lib/feasibility_clamp.py` `clamp_avg_et_to_period(yaml_dir, 0.95)`,
wired as step 4 in `orchestrator.run_full_generation_pipeline` after
`generate_additional_execution_traces`; clamps `mu`+`max`+`min` (NOT mu alone —
`FiniteDist` truncates at `max`, so mu-only is a scored-SP no-op) to
`0.95*period` and relabels `deadline`→`period` for non-perf tasks with
`mu>0.95*period`; perf tasks skipped (TL-grid bounds). 7 tests RED→GREEN + the
integration invariant; 21/21 Gen_Taskset + 291/291 tests/python green. Fires on
exactly 1 task in the N=4 run (taskset_1 gid=2 `mu=21.4>20`); 39/40 already
feasible. REMAINS: user re-runs the A/B to confirm INCR ≥ INCR_WCET + Q3
verdict; H1c writeup now academic).
**P2.4** (Slices A+B + 1-arg overload test migration COMPLETE 2026-07-11, 16/16
ctest DEBUG + 291 python green, staged not committed — optimizer methods &
mode-string refactor; trigger = the `INCR_P1` inversion: `ReoptimizationPeriod=1`
→ reopt every interval, never incremental, the `P<n>` knob runs the wrong way
for a reader. Shipped: stale-comment rewrite + mode-string rename
`INCR_P<n>`→`INCR_Reopt_X` (X∈{1,5,10,30,60}, X=5 NEW; stale old names FAIL
LOUDLY; bare `INCR`=`INCR_Reopt_10`). DEFERRED Slice C (method renames, conflicts
with P1.6/P1.2 in-flight edits). **P2.5** = the `INCR_SCRATCH` removal, promoted
out of P2.4 Step 5 into its own result-changing task — COMPLETE 2026-07-11 (16/16
ctest + 287 python green; deleted the SCRATCH branch + the 1-arg overload +
the E2 gate, narrowed Q1/Q2/Q3/E1 to INCR-only). The user runs the full new-name
A/B without INCR_SCRATCH).
**P1.6** (planning only — pure-incremental RM-fast-bootstrap paper-grade A/B arm;
D2+D5 settled 2026-07-11 = dedicated `scheduler_mode_` branch + A/B probe not an
E3 gate; settle D1/D3/D4 with the user, then TDD the bootstrap + dispatch.
**Premise shifted 2026-07-11 (P2.5):** the original "incr-vs-scratch" contrast is
gone (INCR_SCRATCH removed); the surviving axis is vs `INCR_Reopt_1` (the
always-reopt-with-memory arm) — does the from-scratch descent earn its cost vs.
the pure 1-D incremental walk? Re-framing deferred to the user).
~~P0.1~~ RESOLVED 2026-07-10 (subsumed by P0.5; inspectability write discarded —
see `finished_tasks/summary.md`). Each P0/P1 task is one review-and-commit cycle
per `agent_coding_rules.md`.
~~P0.1~~ RESOLVED 2026-07-10 (subsumed by P0.5; inspectability write discarded —
see `finished_tasks/summary.md`). Each P0/P1 task is one review-and-commit cycle
per `agent_coding_rules.md`.
